#!/usr/bin/env python
from setuptools import setup

setup(name='kvv-simulator',
      version='0.0.1',
      install_requires=['pandas',
                        'networkx',
                        'gym',
                        'cfg_load',
                        ]
      )
